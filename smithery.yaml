# smithery.yaml
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      # You can add configuration options here if needed.
      # For now, leave it empty.
  commandFunction: |-
    (config) => {
      return {
        command: "npm",
        args: ["start"],
        env: {
          // EXA_API_KEY will be provided via environment variable or secret management.
          EXA_API_KEY: process.env.EXA_API_KEY || ""
        }
      };
    }
